This is the first CRAN submission of the `ggtranscript` package.

## R CMD check results

There were no `ERROR`s, `WARNING`s or `NOTE`s.

## Downstream dependencies

There are currently no downstream dependencies for this package.
